#include "stdafx.h"
#include "semantic_validator.h"

std::vector<std::shared_ptr<exprtree>> semantic_validator::validate(const exprtree & tree) {
    auto errors = std::vector<std::shared_ptr<exprtree>>();
    return errors;
}
